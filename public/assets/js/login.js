const loginFormHander = async (event) => {
const email = document.querySelector('#login-username').value.trim();
const password = document.querySelector('#plogin-password').value.trim();

if (email && password) {
    
}

}